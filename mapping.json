{
  "_comment": "Edit `bindings` only — friendly control name -> herdr action. Actions: tab_next, tab_prev, workspace_next, workspace_prev, pane_left, pane_right, pane_up, pane_down, pane_zoom, scroll_up, scroll_down, enter, escape, voice, dictation, noop. Scrolling is analog (right stick) configured under settings.scroll. The `profile` maps each friendly name to the raw SDL index; run `--calibrate` to rebuild it.",

  "bindings": {
    "A": "enter",
    "B": "escape",
    "X": "pane_zoom",
    "Y": "voice",
    "L": "workspace_prev",
    "R": "workspace_next",
    "ZL": "tab_prev",
    "ZR": "tab_next",
    "dpad_up": "pane_up",
    "dpad_down": "pane_down",
    "dpad_left": "pane_left",
    "dpad_right": "pane_right"
  },

  "remote": {
    "_comment": "Rust V2 only: where the herdr relay runs. ssh_host is any valid ssh target (user@host or a ~/.ssh/config alias). relay_cmd is run on the Mac and reads intents on stdin. Leave ssh_host empty to require --host. The Python daemon ignores this block.",
    "ssh_host": "",
    "ssh_args": [],
    "relay_cmd": "cd ~/cc-herdr-controller && python3 relay.py"
  },

  "local": {
    "_comment": "Rust V2 --local mode: run herdr on THIS machine, no SSH. relay_cmd is run via the shell from the repo root (next to herdr.py/relay.py) and reads intents on stdin. The Python daemon ignores this block.",
    "relay_cmd": "python3 relay.py"
  },

  "profile": {
    "_comment": "friendly name -> raw SDL index. Defaults are for the Switch Pro via SDL; if buttons feel wrong, run: python controller_daemon.py --calibrate",
    "buttons": {
      "A": 0, "B": 1, "X": 2, "Y": 3,
      "minus": 4, "home": 5, "plus": 6,
      "lstick": 7, "rstick": 8,
      "L": 9, "R": 10,
      "dpad_up": 11, "dpad_down": 12, "dpad_left": 13, "dpad_right": 14,
      "capture": 15
    },
    "axes": {
      "left_x": 0, "left_y": 1, "right_x": 2, "right_y": 3, "ZL": 4, "ZR": 5
    }
  },

  "settings": {
    "trigger_threshold": 0.5,

    "scroll": {
      "axis": "right_y",
      "invert": false,
      "deadzone": 0.18,
      "tick_ms": 30,
      "max_lines": 6
    },

    "arrows": {
      "axis_x": "left_x",
      "axis_y": "left_y",
      "deadzone": 0.5,
      "repeat_ms": 150
    },

    "voice": {
      "mode": "both",
      "tap_max_ms": 300,
      "repeat_ms": 90,
      "char": " "
    },

    "dictation_command": "echo 'set settings.dictation_command in mapping.json'"
  }
}
